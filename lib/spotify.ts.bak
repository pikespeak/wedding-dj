// Platzhalter-Helper für Spotify-Aufrufe (noch ohne echte Web API Calls)

export type SpotifyDevice = { id: string; name: string; is_active?: boolean }

export async function transferPlayback(deviceId: string) {
  console.log("[spotify] transfer to device:", deviceId)
  return { ok: true }
}

export async function play() {
  console.log("[spotify] play()")
  return { ok: true }
}

export async function pause() {
  console.log("[spotify] pause()")
  return { ok: true }
}

export async function nextTrack() {
  console.log("[spotify] nextTrack()")
  return { ok: true }
}
